<UserControl x:Class="Iris.Framework.MenuBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400" BorderBrush="#FF7F7F7F" BorderThickness="1">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="150"/>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListBox Grid.Row="1" Height="150" Width="390"
                 ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     HorizontalAlignment="Center" Name="listBox" VerticalAlignment="Center"
                 />

        <Button Grid.Row="2" Content="OK" Height="23" HorizontalAlignment="Right" Margin="5" Name="select" VerticalAlignment="Bottom" Width="75" Click="selectAnswer"/>
    </Grid>



</UserControl>
